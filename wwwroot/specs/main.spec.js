describe('Jasmine tests 1', function () {
    it('a successful test', function () {
        //debugger; // This is like setting a breakpoint
		expect(1 + 2).toEqual(3);
	});
	it('another successful test OK', function () {
		expect(1 + 1).toEqual(2);
	});
});

